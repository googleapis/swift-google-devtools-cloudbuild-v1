// Code generated by sidekick. DO NOT EDIT.
//
// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

import Foundation
@_spi(GoogleCloudInternal) import GoogleWKT

/// Optional arguments to enable specific features of builds.
public struct BuildOptions: Codable, Equatable, GoogleWKT._AnyPackable,
  Sendable
{
  /// Requested hash for SourceProvenance.
  public var sourceProvenanceHash: [Hash.HashType] = []

  /// Requested verifiability options.
  public var requestedVerifyOption: BuildOptions.VerifyOption = BuildOptions.VerifyOption()

  /// Compute Engine machine type on which to run the build.
  public var machineType: BuildOptions.MachineType = BuildOptions.MachineType()

  /// Requested disk size for the VM that runs the build. Note that this is *NOT*
  /// "disk free"; some of the space will be used by the operating system and
  /// build utilities. Also note that this is the minimum disk size that will be
  /// allocated for the build -- the build may run with a larger disk than
  /// requested. At present, the maximum disk size is 4000GB; builds that request
  /// more than the maximum are rejected with an error.
  public var diskSizeGb: Swift.Int64 = Swift.Int64()

  /// Option to specify behavior when there is an error in the substitution
  /// checks.
  ///
  /// NOTE: this is always set to ALLOW_LOOSE for triggered builds and cannot
  /// be overridden in the build configuration file.
  public var substitutionOption: BuildOptions.SubstitutionOption = BuildOptions.SubstitutionOption()

  /// Option to specify whether or not to apply bash style string
  /// operations to the substitutions.
  ///
  /// NOTE: this is always enabled for triggered builds and cannot be
  /// overridden in the build configuration file.
  public var dynamicSubstitutions: Swift.Bool = Swift.Bool()

  /// Option to include built-in and custom substitutions as env variables
  /// for all build steps.
  public var automapSubstitutions: Swift.Bool = Swift.Bool()

  /// Option to define build log streaming behavior to Cloud
  /// Storage.
  public var logStreamingOption: BuildOptions.LogStreamingOption = BuildOptions.LogStreamingOption()

  /// This field deprecated; please use `pool.name` instead.
  @available(*, deprecated)
  public var workerPool: Swift.String = Swift.String()

  /// Optional. Specification for execution on a `WorkerPool`.
  ///
  /// See [running builds in a private
  /// pool](https://cloud.google.com/build/docs/private-pools/run-builds-in-private-pool)
  /// for more information.
  public var pool: BuildOptions.PoolOption? = nil

  /// Option to specify the logging mode, which determines if and where build
  /// logs are stored.
  public var logging: BuildOptions.LoggingMode = BuildOptions.LoggingMode()

  /// A list of global environment variable definitions that will exist for all
  /// build steps in this build. If a variable is defined in both globally and in
  /// a build step, the variable will use the build step value.
  ///
  /// The elements are of the form "KEY=VALUE" for the environment variable "KEY"
  /// being given the value "VALUE".
  public var env: [Swift.String] = []

  /// A list of global environment variables, which are encrypted using a Cloud
  /// Key Management Service crypto key. These values must be specified in the
  /// build's `Secret`. These variables will be available to all build steps
  /// in this build.
  public var secretEnv: [Swift.String] = []

  /// Global list of volumes to mount for ALL build steps
  ///
  /// Each volume is created as an empty volume prior to starting the build
  /// process. Upon completion of the build, volumes and their contents are
  /// discarded. Global volume names and paths cannot conflict with the volumes
  /// defined a build step.
  ///
  /// Using a global volume in a build with only one step is not valid as
  /// it is indicative of a build request with an incorrect configuration.
  public var volumes: [Volume] = []

  /// Optional. Option to specify how default logs buckets are setup.
  public var defaultLogsBucketBehavior: BuildOptions.DefaultLogsBucketBehavior =
    BuildOptions.DefaultLogsBucketBehavior()

  /// Optional. Option to specify whether structured logging is enabled.
  ///
  /// If true, JSON-formatted logs are parsed as structured logs.
  public var enableStructuredLogging: Swift.Bool = Swift.Bool()

  @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

  /// Initialize a new instance of `BuildOptions`.
  public init() {}

  /// Use `config` to return a new instance of this object, with some fields updated.
  ///
  /// Commonly used to initialize the value, for example:
  ///
  /// ```
  /// let value = BuildOptions().with { $0.sourceProvenanceHash = ... }
  /// ```
  public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
    var copy = self
    try config(&copy)
    return copy
  }

  private struct CodingKeys: CodingKey {
    var stringValue: Swift.String
    var intValue: Swift.Int? { nil }
    init(stringValue: Swift.String) { self.stringValue = stringValue }
    init?(intValue: Swift.Int) { nil }

    static let sourceProvenanceHash = CodingKeys(stringValue: "sourceProvenanceHash")
    static let requestedVerifyOption = CodingKeys(stringValue: "requestedVerifyOption")
    static let machineType = CodingKeys(stringValue: "machineType")
    static let diskSizeGb = CodingKeys(stringValue: "diskSizeGb")
    static let substitutionOption = CodingKeys(stringValue: "substitutionOption")
    static let dynamicSubstitutions = CodingKeys(stringValue: "dynamicSubstitutions")
    static let automapSubstitutions = CodingKeys(stringValue: "automapSubstitutions")
    static let logStreamingOption = CodingKeys(stringValue: "logStreamingOption")
    static let workerPool = CodingKeys(stringValue: "workerPool")
    static let pool = CodingKeys(stringValue: "pool")
    static let logging = CodingKeys(stringValue: "logging")
    static let env = CodingKeys(stringValue: "env")
    static let secretEnv = CodingKeys(stringValue: "secretEnv")
    static let volumes = CodingKeys(stringValue: "volumes")
    static let defaultLogsBucketBehavior = CodingKeys(stringValue: "defaultLogsBucketBehavior")
    static let enableStructuredLogging = CodingKeys(stringValue: "enableStructuredLogging")

    static let _knownKeys: Set<Swift.String> = [
      "sourceProvenanceHash",
      "requestedVerifyOption",
      "machineType",
      "diskSizeGb",
      "substitutionOption",
      "dynamicSubstitutions",
      "automapSubstitutions",
      "logStreamingOption",
      "workerPool",
      "pool",
      "logging",
      "env",
      "secretEnv",
      "volumes",
      "defaultLogsBucketBehavior",
      "enableStructuredLogging",
    ]
  }

  #if hasAttribute(diagnose)
    @diagnose(DeprecatedDeclaration, as: ignored)
  #endif
  public init(from decoder: Decoder) throws {
    let container = try decoder.container(keyedBy: CodingKeys.self)
    if let value = try container.decodeIfPresent(
      [Hash.HashType].self, forKey: .sourceProvenanceHash)
    {
      self.sourceProvenanceHash = value
    }
    if let value = try container.decodeIfPresent(
      BuildOptions.VerifyOption.self, forKey: .requestedVerifyOption)
    {
      self.requestedVerifyOption = value
    }
    if let value = try container.decodeIfPresent(
      BuildOptions.MachineType.self, forKey: .machineType)
    {
      self.machineType = value
    }
    if let value = try container.decodeIfPresent(Swift.Int64.self, forKey: .diskSizeGb) {
      self.diskSizeGb = value
    }
    if let value = try container.decodeIfPresent(
      BuildOptions.SubstitutionOption.self, forKey: .substitutionOption)
    {
      self.substitutionOption = value
    }
    if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .dynamicSubstitutions) {
      self.dynamicSubstitutions = value
    }
    if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .automapSubstitutions) {
      self.automapSubstitutions = value
    }
    if let value = try container.decodeIfPresent(
      BuildOptions.LogStreamingOption.self, forKey: .logStreamingOption)
    {
      self.logStreamingOption = value
    }
    if let value = try container.decodeIfPresent(Swift.String.self, forKey: .workerPool) {
      self.workerPool = value
    }
    self.pool = try container.decodeIfPresent(BuildOptions.PoolOption.self, forKey: .pool)
    if let value = try container.decodeIfPresent(BuildOptions.LoggingMode.self, forKey: .logging) {
      self.logging = value
    }
    if let value = try container.decodeIfPresent([Swift.String].self, forKey: .env) {
      self.env = value
    }
    if let value = try container.decodeIfPresent([Swift.String].self, forKey: .secretEnv) {
      self.secretEnv = value
    }
    if let value = try container.decodeIfPresent([Volume].self, forKey: .volumes) {
      self.volumes = value
    }
    if let value = try container.decodeIfPresent(
      BuildOptions.DefaultLogsBucketBehavior.self, forKey: .defaultLogsBucketBehavior)
    {
      self.defaultLogsBucketBehavior = value
    }
    if let value = try container.decodeIfPresent(Swift.Bool.self, forKey: .enableStructuredLogging)
    {
      self.enableStructuredLogging = value
    }
    for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
      self._unknownFields.json[key.stringValue] = try container.decode(
        GoogleWKT.Value.self, forKey: key)
    }
  }

  #if hasAttribute(diagnose)
    @diagnose(DeprecatedDeclaration, as: ignored)
  #endif
  public func encode(to encoder: Encoder) throws {
    var container = encoder.container(keyedBy: CodingKeys.self)
    try container.encode(self.sourceProvenanceHash, forKey: .sourceProvenanceHash)
    try container.encode(self.requestedVerifyOption, forKey: .requestedVerifyOption)
    try container.encode(self.machineType, forKey: .machineType)
    try container.encode(self.diskSizeGb, forKey: .diskSizeGb)
    try container.encode(self.substitutionOption, forKey: .substitutionOption)
    try container.encode(self.dynamicSubstitutions, forKey: .dynamicSubstitutions)
    try container.encode(self.automapSubstitutions, forKey: .automapSubstitutions)
    try container.encode(self.logStreamingOption, forKey: .logStreamingOption)
    try container.encode(self.workerPool, forKey: .workerPool)
    try container.encodeIfPresent(self.pool, forKey: .pool)
    try container.encode(self.logging, forKey: .logging)
    try container.encode(self.env, forKey: .env)
    try container.encode(self.secretEnv, forKey: .secretEnv)
    try container.encode(self.volumes, forKey: .volumes)
    try container.encode(self.defaultLogsBucketBehavior, forKey: .defaultLogsBucketBehavior)
    try container.encode(self.enableStructuredLogging, forKey: .enableStructuredLogging)
    for (key, value) in self._unknownFields.json {
      try container.encode(value, forKey: CodingKeys(stringValue: key))
    }
  }

  /// Details about how a build should be executed on a `WorkerPool`.
  ///
  /// See [running builds in a private
  /// pool](https://cloud.google.com/build/docs/private-pools/run-builds-in-private-pool)
  /// for more information.
  public struct PoolOption: Codable, Equatable, GoogleWKT._AnyPackable,
    Sendable
  {
    /// The `WorkerPool` resource to execute the build on.
    /// You must have `cloudbuild.workerpools.use` on the project hosting the
    /// WorkerPool.
    ///
    /// Format projects/{project}/locations/{location}/workerPools/{workerPoolId}
    public var name: Swift.String = Swift.String()

    @_spi(GoogleCloudInternal) public var _unknownFields: GoogleWKT._UnknownFields = .init()

    /// Initialize a new instance of `PoolOption`.
    public init() {}

    /// Use `config` to return a new instance of this object, with some fields updated.
    ///
    /// Commonly used to initialize the value, for example:
    ///
    /// ```
    /// let value = PoolOption().with { $0.name = ... }
    /// ```
    public func with(_ config: (inout Self) throws -> Swift.Void) rethrows -> Self {
      var copy = self
      try config(&copy)
      return copy
    }

    private struct CodingKeys: CodingKey {
      var stringValue: Swift.String
      var intValue: Swift.Int? { nil }
      init(stringValue: Swift.String) { self.stringValue = stringValue }
      init?(intValue: Swift.Int) { nil }

      static let name = CodingKeys(stringValue: "name")

      static let _knownKeys: Set<Swift.String> = [
        "name"
      ]
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.container(keyedBy: CodingKeys.self)
      if let value = try container.decodeIfPresent(Swift.String.self, forKey: .name) {
        self.name = value
      }
      for key in container.allKeys where !CodingKeys._knownKeys.contains(key.stringValue) {
        self._unknownFields.json[key.stringValue] = try container.decode(
          GoogleWKT.Value.self, forKey: key)
      }
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.container(keyedBy: CodingKeys.self)
      try container.encode(self.name, forKey: .name)
      for (key, value) in self._unknownFields.json {
        try container.encode(value, forKey: CodingKeys(stringValue: key))
      }
    }

    public static var _anyTypeUrl: Swift.String {
      return "type.googleapis.com/google.devtools.cloudbuild.v1.BuildOptions.PoolOption"
    }
    public init(fromAny any: GoogleWKT.`Any`) throws {
      self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
    }
    public func _pack() throws -> GoogleWKT.Struct {
      return try GoogleWKT._slowAnySerialize(message: self)
    }
  }

  /// Specifies the manner in which the build should be verified, if at all.
  ///
  /// If a verified build is requested, and any part of the process to generate
  /// and upload provenance fails, the build will also fail.
  ///
  /// If the build does not request verification then that process may occur, but
  /// is not guaranteed to. If it does occur and fails, the build will not fail.
  ///
  /// For more information, see [Viewing Build
  /// Provenance](https://cloud.google.com/build/docs/securing-builds/view-build-provenance).
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum VerifyOption: Codable, Equatable, Sendable {
    /// Not a verifiable build (the default).
    case notVerified
    /// Build must be verified.
    case verified
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .notVerified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .notVerified: return 0
      case .verified: return 1
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .notVerified: return "NOT_VERIFIED"
      case .verified: return "VERIFIED"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:VerifyOption/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "NOT_VERIFIED": self = .notVerified
      case "VERIFIED": self = .verified
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:VerifyOption/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .notVerified
      case 1: self = .verified
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .notVerified: return try container.encode("NOT_VERIFIED")
      case .verified: return try container.encode("VERIFIED")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Supported Compute Engine machine types.
  /// For more information, see [Machine
  /// types](https://cloud.google.com/compute/docs/machine-types).
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum MachineType: Codable, Equatable, Sendable {
    /// Standard machine type.
    case unspecified
    /// Highcpu machine with 8 CPUs.
    @available(*, deprecated)
    case n1Highcpu8
    /// Highcpu machine with 32 CPUs.
    @available(*, deprecated)
    case n1Highcpu32
    /// Highcpu e2 machine with 8 CPUs.
    case e2Highcpu8
    /// Highcpu e2 machine with 32 CPUs.
    case e2Highcpu32
    /// E2 machine with 1 CPU.
    case e2Medium
    /// E2 machine with 2 CPUs.
    case e2Standard2
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .n1Highcpu8: return 1
      case .n1Highcpu32: return 2
      case .e2Highcpu8: return 5
      case .e2Highcpu32: return 6
      case .e2Medium: return 7
      case .e2Standard2: return 11
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "UNSPECIFIED"
      case .n1Highcpu8: return "N1_HIGHCPU_8"
      case .n1Highcpu32: return "N1_HIGHCPU_32"
      case .e2Highcpu8: return "E2_HIGHCPU_8"
      case .e2Highcpu32: return "E2_HIGHCPU_32"
      case .e2Medium: return "E2_MEDIUM"
      case .e2Standard2: return "E2_STANDARD_2"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:MachineType/unknownStringValue(_:)).
    #if hasAttribute(diagnose)
      @diagnose(DeprecatedDeclaration, as: ignored)
    #endif
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "UNSPECIFIED": self = .unspecified
      case "N1_HIGHCPU_8": self = .n1Highcpu8
      case "N1_HIGHCPU_32": self = .n1Highcpu32
      case "E2_HIGHCPU_8": self = .e2Highcpu8
      case "E2_HIGHCPU_32": self = .e2Highcpu32
      case "E2_MEDIUM": self = .e2Medium
      case "E2_STANDARD_2": self = .e2Standard2
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:MachineType/unknownIntValue(_:)).
    #if hasAttribute(diagnose)
      @diagnose(DeprecatedDeclaration, as: ignored)
    #endif
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .n1Highcpu8
      case 2: self = .n1Highcpu32
      case 5: self = .e2Highcpu8
      case 6: self = .e2Highcpu32
      case 7: self = .e2Medium
      case 11: self = .e2Standard2
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("UNSPECIFIED")
      case .n1Highcpu8: return try container.encode("N1_HIGHCPU_8")
      case .n1Highcpu32: return try container.encode("N1_HIGHCPU_32")
      case .e2Highcpu8: return try container.encode("E2_HIGHCPU_8")
      case .e2Highcpu32: return try container.encode("E2_HIGHCPU_32")
      case .e2Medium: return try container.encode("E2_MEDIUM")
      case .e2Standard2: return try container.encode("E2_STANDARD_2")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Specifies the behavior when there is an error in the substitution checks.
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum SubstitutionOption: Codable, Equatable, Sendable {
    /// Fails the build if error in substitutions checks, like missing
    /// a substitution in the template or in the map.
    case mustMatch
    /// Do not fail the build if error in substitutions checks.
    case allowLoose
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .mustMatch
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .mustMatch: return 0
      case .allowLoose: return 1
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .mustMatch: return "MUST_MATCH"
      case .allowLoose: return "ALLOW_LOOSE"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:SubstitutionOption/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "MUST_MATCH": self = .mustMatch
      case "ALLOW_LOOSE": self = .allowLoose
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:SubstitutionOption/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .mustMatch
      case 1: self = .allowLoose
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .mustMatch: return try container.encode("MUST_MATCH")
      case .allowLoose: return try container.encode("ALLOW_LOOSE")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Specifies the behavior when writing build logs to Cloud Storage.
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum LogStreamingOption: Codable, Equatable, Sendable {
    /// Service may automatically determine build log streaming behavior.
    case streamDefault
    /// Build logs should be streamed to Cloud Storage.
    case streamOn
    /// Build logs should not be streamed to Cloud Storage; they will be
    /// written when the build is completed.
    case streamOff
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .streamDefault
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .streamDefault: return 0
      case .streamOn: return 1
      case .streamOff: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .streamDefault: return "STREAM_DEFAULT"
      case .streamOn: return "STREAM_ON"
      case .streamOff: return "STREAM_OFF"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:LogStreamingOption/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "STREAM_DEFAULT": self = .streamDefault
      case "STREAM_ON": self = .streamOn
      case "STREAM_OFF": self = .streamOff
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:LogStreamingOption/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .streamDefault
      case 1: self = .streamOn
      case 2: self = .streamOff
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .streamDefault: return try container.encode("STREAM_DEFAULT")
      case .streamOn: return try container.encode("STREAM_ON")
      case .streamOff: return try container.encode("STREAM_OFF")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Specifies the logging mode.
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum LoggingMode: Codable, Equatable, Sendable {
    /// The service determines the logging mode. The default is `LEGACY`. Do not
    /// rely on the default logging behavior as it may change in the future.
    case loggingUnspecified
    /// Build logs are stored in Cloud Logging and Cloud Storage.
    case legacy
    /// Build logs are stored in Cloud Storage.
    case gcsOnly
    /// This option is the same as CLOUD_LOGGING_ONLY.
    @available(*, deprecated)
    case stackdriverOnly
    /// Turn off all logging. No build logs will be captured.
    case `none`
    /// Build logs are stored in Cloud Logging. Selecting this option will not
    /// allow [logs
    /// streaming](https://cloud.google.com/sdk/gcloud/reference/builds/log).
    case cloudLoggingOnly
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .loggingUnspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .loggingUnspecified: return 0
      case .legacy: return 1
      case .gcsOnly: return 2
      case .stackdriverOnly: return 3
      case .`none`: return 4
      case .cloudLoggingOnly: return 5
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .loggingUnspecified: return "LOGGING_UNSPECIFIED"
      case .legacy: return "LEGACY"
      case .gcsOnly: return "GCS_ONLY"
      case .stackdriverOnly: return "STACKDRIVER_ONLY"
      case .`none`: return "NONE"
      case .cloudLoggingOnly: return "CLOUD_LOGGING_ONLY"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:LoggingMode/unknownStringValue(_:)).
    #if hasAttribute(diagnose)
      @diagnose(DeprecatedDeclaration, as: ignored)
    #endif
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "LOGGING_UNSPECIFIED": self = .loggingUnspecified
      case "LEGACY": self = .legacy
      case "GCS_ONLY": self = .gcsOnly
      case "STACKDRIVER_ONLY": self = .stackdriverOnly
      case "CLOUD_LOGGING_ONLY": self = .cloudLoggingOnly
      case "NONE": self = .`none`
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:LoggingMode/unknownIntValue(_:)).
    #if hasAttribute(diagnose)
      @diagnose(DeprecatedDeclaration, as: ignored)
    #endif
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .loggingUnspecified
      case 1: self = .legacy
      case 2: self = .gcsOnly
      case 3: self = .stackdriverOnly
      case 4: self = .`none`
      case 5: self = .cloudLoggingOnly
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .loggingUnspecified: return try container.encode("LOGGING_UNSPECIFIED")
      case .legacy: return try container.encode("LEGACY")
      case .gcsOnly: return try container.encode("GCS_ONLY")
      case .stackdriverOnly: return try container.encode("STACKDRIVER_ONLY")
      case .`none`: return try container.encode("NONE")
      case .cloudLoggingOnly: return try container.encode("CLOUD_LOGGING_ONLY")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  /// Default Cloud Storage log bucket behavior options.
  ///
  /// - Note: Adding cases to this enumeration is not considered a breaking change.
  ///   Always include an `@unknown default:` case when switching over this type.
  ///   Do not pattern-match against `unknownStringValue` or `unknownIntValue`
  ///   expecting specific values to remain unparsed; future releases may promote
  ///   them to named cases.
  public enum DefaultLogsBucketBehavior: Codable, Equatable, Sendable {
    /// Unspecified.
    case unspecified
    /// Bucket is located in user-owned project in the same region as the
    /// build. The builder service account must have access to create and write
    /// to Cloud Storage buckets in the build project.
    case regionalUserOwnedBucket
    /// Bucket is located in a Google-owned project and is not regionalized.
    case legacyBucket
    /// Encodes an unknown integer value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific integer values in this case;
    ///   future releases may promote them to named enum cases.
    case unknownIntValue(Int)
    /// Encodes an unknown string value.
    ///
    /// The most common cause for an unknown value is for the service to send
    /// a value unknown to the library. We recommend you update your library to
    /// the latest version.
    ///
    /// - Warning: Do not pattern-match specific string literals in this case;
    ///   future releases may promote them to named enum cases.
    case unknownStringValue(String)

    public init() {
      self = .unspecified
    }

    /// Returns the integer value associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown string value, this returns `nil`.
    public var intValue: Int? {
      switch self {
      case .unspecified: return 0
      case .regionalUserOwnedBucket: return 1
      case .legacyBucket: return 2
      case .unknownIntValue(let v): return v
      case .unknownStringValue: return nil
      }
    }

    /// Returns the string value (or name) associated with the enumeration.
    ///
    /// If the enumeration was initialized with an unknown integer value, this returns `nil`.
    public var stringValue: Swift.String? {
      switch self {
      case .unspecified: return "DEFAULT_LOGS_BUCKET_BEHAVIOR_UNSPECIFIED"
      case .regionalUserOwnedBucket: return "REGIONAL_USER_OWNED_BUCKET"
      case .legacyBucket: return "LEGACY_BUCKET"
      case .unknownIntValue: return nil
      case .unknownStringValue(let v): return v
      }
    }

    /// Initialize from a string value.
    ///
    /// If the value is unknown, this initializes to [`unknownStringValue`](doc:DefaultLogsBucketBehavior/unknownStringValue(_:)).
    public init(stringValue: Swift.String) {
      switch stringValue {
      case "DEFAULT_LOGS_BUCKET_BEHAVIOR_UNSPECIFIED": self = .unspecified
      case "REGIONAL_USER_OWNED_BUCKET": self = .regionalUserOwnedBucket
      case "LEGACY_BUCKET": self = .legacyBucket
      default: self = .unknownStringValue(stringValue)
      }
    }

    /// Initialize from an integer value.
    ///
    /// If the value is unknown, this initializes to [`unknownIntValue`](doc:DefaultLogsBucketBehavior/unknownIntValue(_:)).
    public init(intValue: Int) {
      switch intValue {
      case 0: self = .unspecified
      case 1: self = .regionalUserOwnedBucket
      case 2: self = .legacyBucket
      default: self = .unknownIntValue(intValue)
      }
    }

    public init(from decoder: Decoder) throws {
      let container = try decoder.singleValueContainer()
      if let v = try? container.decode(Int.self) {
        self.init(intValue: v)
        return
      }
      if let s = try? container.decode(String.self) {
        if let v = Int(s) {
          self.init(intValue: v)
        } else {
          self.init(stringValue: s)
        }
        return
      }
      throw DecodingError.dataCorruptedError(
        in: container, debugDescription: "Expected enum value, must be integer or string.")
    }

    public func encode(to encoder: Encoder) throws {
      var container = encoder.singleValueContainer()
      switch self {
      case .unspecified: return try container.encode("DEFAULT_LOGS_BUCKET_BEHAVIOR_UNSPECIFIED")
      case .regionalUserOwnedBucket: return try container.encode("REGIONAL_USER_OWNED_BUCKET")
      case .legacyBucket: return try container.encode("LEGACY_BUCKET")
      case .unknownIntValue(let v): return try container.encode(v)
      case .unknownStringValue(let v): return try container.encode(v)
      }
    }
  }

  public static var _anyTypeUrl: Swift.String {
    return "type.googleapis.com/google.devtools.cloudbuild.v1.BuildOptions"
  }
  public init(fromAny any: GoogleWKT.`Any`) throws {
    self = try GoogleWKT._slowAnyDeserialize(Self.self, from: any)
  }
  public func _pack() throws -> GoogleWKT.Struct {
    return try GoogleWKT._slowAnySerialize(message: self)
  }
}
